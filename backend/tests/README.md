# Backend Tests

This folder is reserved for backend tests.

Suggested structure:

- `backend/tests/controllers/`
- `backend/tests/services/`
- `backend/tests/routes/`

The project is currently using frontend test coverage first, but the backend is now separated and ready for unit or integration tests when we add a test runner.
